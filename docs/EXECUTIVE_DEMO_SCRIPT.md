# 5-minute Executive Demo

1. **Overview (45 sec)** — Show total VM, network flows, TCP failures, expiry/EOSL risks. Explain that this is one operational view over infrastructure data already owned by the RPA team.
2. **Architecture (90 sec)** — Open grouped view. Explain that 30+ VMs are intentionally collapsed into service tiers. Double-click APP tier to drill into only those VMs.
3. **Live connection (90 sec)** — Click a connection. Left Drawer shows approved policy and source-side Telegraf actual probe together. Show Ping UP/TCP DOWN sample and state that the portal narrows investigation without falsely declaring a firewall root cause.
4. **Expiry risk (45 sec)** — Show D-day and expired-but-still-reachable policy. Explain proactive renewal and declared/actual mismatch detection.
5. **VM Drawer (30 sec)** — Click VM and show resource snapshot, software/EOSL, related network and SOP/Grafana links.

Message: **"정책상 열려 있어야 하는 연결과 실제 통신 상태를 한 화면에서 비교하고, 장애/만료/EOSL까지 운영 관점에서 연결합니다."**
